<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- regular -->
    <font
        android:font="@font/major_mono_display_regular"
        android:fontStyle="normal"
        android:fontWeight="400" />

    <!-- italic -->
    <font
        android:font="@font/roboto_slab_thin"
        android:fontStyle="italic"
        android:fontWeight="400" />

    <!-- bold -->
    <font
        android:font="@font/dancing_script_bold"
        android:fontStyle="normal"
        android:fontWeight="700" />

    <!-- bold italic -->
    <font
        android:font="@font/patrick_hand_regular"
        android:fontStyle="italic"
        android:fontWeight="700" />
</font-family>